# Example .continue/mcpServers/node-sandbox-mcp.yaml
name: Node Sandbox MCP
version: 1.0.0
schema: v1
mcpServers:
  - name: node-sandbox
    command: npx
    args:
      - -y
      - node-code-sandbox-mcp
